import {describe, expect, test} from 'vitest'

//para saber si está activo el componente del cubo
describe('JuanJose', () => {
    test("Deberia de mostrar los numeros", () => {
        expect(1+1).toBe(2)
    })
})